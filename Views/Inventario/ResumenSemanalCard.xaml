<UserControl x:Class="ValeriosPizza.Views.Inventario.ResumenSemanalCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="1000">
    <Border Style="{StaticResource CardStyle}" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="RESUMEN SEMANAL" FontSize="12" Foreground="{DynamicResource TextMutedBrush}" FontWeight="Medium" Margin="0,0,0,10"/>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding DiasSemana}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7" Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="2" Padding="10" CornerRadius="4"
                                Background="{DynamicResource OtherDayBackgroundBrush}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsToday}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource TodayHighlightBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DiaNombre}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" FontWeight="Medium"/>
                                <TextBlock Text="{Binding Fecha}" FontSize="12" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,2,0,4"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="E:" FontSize="11" Foreground="#4CAF50" Margin="0,0,2,0"/>
                                    <TextBlock Text="{Binding Entradas}" FontSize="11" Foreground="#4CAF50" Margin="0,0,6,0"/>
                                    <TextBlock Text="G:" FontSize="11" Foreground="#2196F3"/>
                                    <TextBlock Text="{Binding Gastos}" FontSize="11" Foreground="#2196F3"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,2,0,0">
                                    <TextBlock Text="M:" FontSize="11" Foreground="#FF9800" Margin="0,0,2,0"/>
                                    <TextBlock Text="{Binding Mermas}" FontSize="11" Foreground="#FF9800" Margin="0,0,6,0"/>
                                    <TextBlock Text="C:" FontSize="11" Foreground="#9C27B0"/>
                                    <TextBlock Text="{Binding Cortesias}" FontSize="11" Foreground="#9C27B0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
