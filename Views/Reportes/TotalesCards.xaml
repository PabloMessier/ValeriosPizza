<UserControl x:Class="ValeriosPizza.Views.Reportes.TotalesCards"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="1000">
    <UniformGrid Columns="8">
        <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0" Padding="15">
            <StackPanel>
                <TextBlock Text="ENTRADAS" FontSize="11" Foreground="#4CAF50" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalEntradas}" FontSize="32" Foreground="#4CAF50" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="4,0,4,0" Padding="15">
            <StackPanel>
                <TextBlock Text="GASTOS" FontSize="11" Foreground="#2196F3" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalGastos}" FontSize="32" Foreground="#2196F3" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="4,0,4,0" Padding="15">
            <StackPanel>
                <TextBlock Text="MERMAS" FontSize="11" Foreground="#FF9800" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalMermas}" FontSize="32" Foreground="#FF9800" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="4,0,4,0" Padding="15">
            <StackPanel>
                <TextBlock Text="CORTESÍAS" FontSize="11" Foreground="#9C27B0" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalCortesias}" FontSize="32" Foreground="#9C27B0" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="4,0,4,0" Padding="15">
            <StackPanel>
                <TextBlock Text="MERCANCÍA" FontSize="11" Foreground="#00BCD4" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalMercancias}" FontSize="32" Foreground="#00BCD4" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="4,0,4,0" Padding="15">
            <StackPanel>
                <TextBlock Text="CAJAS" FontSize="11" Foreground="#795548" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalCajas}" FontSize="32" Foreground="#795548" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="4,0,4,0" Padding="15">
            <StackPanel>
                <TextBlock Text="CONTEOS" FontSize="11" Foreground="#3F51B5" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalConteos}" FontSize="32" Foreground="#3F51B5" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}" Margin="8,0,0,0" Padding="15" Background="{DynamicResource AccentBrush}">
            <StackPanel>
                <TextBlock Text="TOTAL" FontSize="11" Foreground="#888" FontWeight="Medium"/>
                <TextBlock Text="{Binding TotalMovimientos}" FontSize="32" Foreground="White" FontWeight="Light" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
    </UniformGrid>
</UserControl>
