<UserControl x:Class="ValeriosPizza.Views.RegistroRapido.CajasCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="1000">
    <Border Style="{StaticResource CardStyle}" Padding="15" Background="#3E2723">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="CAJAS INICIAL" FontSize="11" Foreground="#FFCCBC" FontWeight="Medium"/>
                    <TextBox Text="{Binding CajaInicial, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" FontSize="14" Margin="0,4,0,0" Background="{DynamicResource CardBackgroundBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="8,0,8,0">
                    <TextBlock Text="RECIBIDAS" FontSize="11" Foreground="#FFCCBC" FontWeight="Medium"/>
                    <TextBox Text="{Binding CajaRecibidas, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" FontSize="14" Margin="0,4,0,0" Background="{DynamicResource CardBackgroundBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="8,0,8,0">
                    <TextBlock Text="UTILIZADAS" FontSize="11" Foreground="#FFCCBC" FontWeight="Medium"/>
                    <TextBox Text="{Binding CajaUtilizadas, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" FontSize="14" Margin="0,4,0,0" Background="{DynamicResource CardBackgroundBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="8,0,8,0">
                    <TextBlock Text="MERMA" FontSize="11" Foreground="#FFCCBC" FontWeight="Medium"/>
                    <TextBox Text="{Binding CajaMerma, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" FontSize="14" Margin="0,4,0,0" Background="{DynamicResource CardBackgroundBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="8,0,8,0" VerticalAlignment="Bottom">
                    <TextBlock Text="30 CM" FontSize="10" Foreground="#FFCCBC" Margin="0,0,0,4"/>
                    <TextBlock Text="CAJAS PARA PIZZAS" FontSize="11" Foreground="#FFCCBC" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Grid.Column="5" Margin="15,0,15,0" VerticalAlignment="Center">
                    <TextBlock Text="DISPONIBLES" FontSize="11" Foreground="#FFCCBC" FontWeight="Medium"/>
                    <TextBlock Text="{Binding CajaDisponible}" FontSize="32" Foreground="White" FontWeight="Light"/>
                </StackPanel>
                <Button Grid.Column="6" Content="GUARDAR CAJAS" Command="{Binding GuardarCajasCommand}"
                        Background="{DynamicResource CardBackgroundBrush}" Foreground="#3E2723" Padding="18,12" FontSize="12"
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
